# DataSource settings
spring.datasource.url=jdbc:mysql://localhost:3306/course_tracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=reroll

# JPA/Hibernate settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true  # Định dạng SQL cho dễ đọc (tùy chọn)

# Server settings
server.port=8080